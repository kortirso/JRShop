== README

Building app:

git clone https://github.com/kortirso/JRShop.git
cd JRShop
bundle install
bundle exec figaro install
set user and database in config/application.yml
- SHOP_DATABASE_USERNAME: 'your_username'
- SHOP_DATABASE_PASSWORD: 'your_password'
rake db:migrate